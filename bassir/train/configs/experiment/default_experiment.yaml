name: default_experiment

train_specs:
  log_every_n_steps:
  max_epochs: 100
  accelerator: "gpu"
  devices: 2
  precision: "bf16-mixed"
  early_stopping_patience: 10

test_specs:
  accelerator: "gpu"
  devices: 1