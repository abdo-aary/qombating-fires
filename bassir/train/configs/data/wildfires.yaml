name: wildfires
specs:
  window_size: 7               # A window of 1 week
  bal_factor: 7
  augmentation_factor: 7
defaults:
  - loader: default_loader      # this will look for data/loader/default_loader.yaml